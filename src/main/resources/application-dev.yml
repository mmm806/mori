spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5434}/${DB_NAME:mori}
    username: ${DB_USER:raymond}
    password: ${DB_PASS:mori1234}
  jpa:
    hibernate:
      ddl-auto: validate

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace
management:
  endpoint:
    health:
      show-details: always
