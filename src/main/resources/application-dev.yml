spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5434}/${DB_NAME:mori_dev}
    username: ${DB_USER:raymond_dev}
    password: ${DB_PASS:devpass}
  jpa:
    properties:
      hibernate.format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace

management:
  endpoint.health.show-details: always
